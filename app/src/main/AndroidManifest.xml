<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.assignment3"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Assignment2">


        <activity android:name=".View.LoginActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".View.RegisterActivity">

        </activity>

        <activity android:name=".View.SearchViewActivity"
        android:exported="true">

    </activity>

        <activity android:name=".View.EditMovieActivity"

            />

        <activity android:name=".View.MovieListActivity"
            android:exported="true">

        </activity>

        <activity android:name=".View.DetailedViewActivity">


    </activity>

    </application>

</manifest>